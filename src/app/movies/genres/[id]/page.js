import React from "react";

const GenreIdPage = ({ params }) => {
  console.log(params);
  return (
    <div>
      <p>Genre id: {params.id}</p>
    </div>
  );
};

export default GenreIdPage;
{
  params;
}
